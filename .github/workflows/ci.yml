name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Maven Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Checkout StreamTask
        uses: actions/checkout@v4
        with:
          repository: C1ouDreamW/StreamTask
          path: StreamTask

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Install StreamTask starter
        working-directory: StreamTask
        run: |
          mvn -B -pl stream-task-spring-boot-starter -am -DskipTests install

      - name: Run tests
        run: |
          chmod +x mvnw
          ./mvnw -B test
